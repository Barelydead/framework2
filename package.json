{
    "name": "redovisa",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "taskkill": "taskkill /f /im node.exe",
        "dev-start": "nodemon ./bin/www",
        "start": "node ./bin/www",
        "docker-6": "docker-compose -f docker-compose-version.yml run node6 npm test",
        "docker-8": "docker-compose -f docker-compose-version.yml run node8 npm test",
        "docker-9": "docker-compose -f docker-compose-version.yml run node9 npm test",
        "docker-test-all": "npm run docker-6 && npm run docker-8 && npm run docker-9",
        "docker-start-v": "docker-compose -f docker-compose-version.yml up -d",
        "docker-stop-v": "docker-compose -f docker-compose-version.yml up -d",
        "start-docker": "docker-compose up -d",
        "stop-docker": "docker-compose down",
        "test-scrutinizer": "nyc --all --reporter=clover mocha --timeout test/**/*.js",
        "report-coveralls": "nyc report --reporter=text-lcov | coveralls",
        "report-codecov": "nyc report --reporter=lcov > coverage.lcov && codecov",
        "test": "nyc --reporter=html --reporter=text mocha --timeout 5000 test/**/*.js",
        "eslint": "eslint .",
        "stylelint": "stylelint **/*.css",
        "lint": "npm run eslint && npm run stylelint"
    },
    "dependencies": {
        "body-parser": "~1.17.1",
        "cookie-parser": "~1.4.3",
        "debug": "~2.6.3",
        "express": "~4.15.2",
        "jstransformer-markdown-it": "^2.0.0",
        "mongodb": "^2.2.33",
        "morgan": "~1.8.1",
        "pug": "~2.0.0-beta11",
        "serve-favicon": "~2.4.2",
        "socket.io": "^2.0.4",
        "test": "^0.6.0"
    },
    "devDependencies": {
        "eslint": "^4.10.0",
        "eslint-plugin-react": "^7.4.0",
        "js-yaml": "^3.10.0",
        "jsonlint": "^1.6.2",
        "mocha": "^4.0.1",
        "nyc": "^11.3.0",
        "codecov": "^3.0.0",
        "coveralls": "^3.0.0",
        "stylelint": "^8.2.0",
        "uglify-js": "^3.1.6"
    }
}
